# Rust Library

This example uses the Stele Rust library.

In contrast to the other examples, this starts the bar in the same process and
does not rely on an IPC socket to update it.

## Usage

```bash
# Run update script.
cargo run --example rust_lib
```
